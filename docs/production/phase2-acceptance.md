# 生产化第二阶段验收记录

记录日期：2026-07-28

目标环境：`executive-ai-local-demo`

冻结基线：`production-foundation-v1.0.0`

开发分支：`codex/production-intelligence-phase2`

## 结论

生产化第二阶段已在本机真实运行环境完成技术验收。数据库迁移、标准脱敏源库、确定性演示数据、每日同步、受控经营问数、当前会话文件问答、Hermes 0.19.0 编排、Anspire 全球模型通道、工作台状态与证据展示均已通过自动化和端到端验证。

本机环境仅用于演示：业务数据全部为可重复生成的模拟数据，界面和回答均明确标注“演示模拟数据”。客户部署模板保持空白，不包含演示账号、模拟数据或模型凭证。

## 交付矩阵

| 计划项 | 已交付内容 | 验收结果 |
|---|---|---:|
| P2-0 基线冻结 | 第一阶段标签、独立二阶段分支、备份与恢复链 | 通过 |
| P2-1 脱敏源库 | PostgreSQL 15–17 标准 ODS、Schema 版本、白名单字段、只读连接 | 通过 |
| P2-2 演示数据 | 6 事业部、45 人、600 客户、3000 商机、800 项目、12000 回款、600 目标、24 个月历史 | 通过 |
| P2-3 飞书模拟 SA | 初始化发布工具、运行期只读 Connector、分页和幂等 upsert | 通过；真实飞书租户凭证留作部署配置 |
| P2-4 自动化任务 | Scheduler、窗口幂等、租约恢复、退避重试、人工立即同步 | 通过 |
| P2-5 标准业务库 | 四数据域独立暂存与激活、快照、能力矩阵、新鲜度 | 通过 |
| P2-6 工具与证据 | 11 个参数化只读工具、权限范围、关键数字证据链 | 通过 |
| P2-7 文件解析 | PDF、DOCX、XLSX、PPTX、混合检索、会话隔离和定位引用 | 通过 |
| P2-8 Hermes | 固定 0.19.0、强制路由、受控 Profile、签名与防重放 | 通过 |
| P2-9 工作台 | SSE、真实处理状态、结构化回答、图表、来源与证据下钻 | 通过 |
| Anspire 单通道 | 全球网关、全量模型目录、GLM-5.2 默认、加密凭证、无供应商降级 | 通过 |
| P2-10 发布验收 | 全量测试、容器 smoke、浏览器验收、备份、客户模板和发布保护 | 通过 |

## Anspire 全球模型通道

- 唯一模型供应商为 Anspire，正式网关固定为 `https://open-gateway.anspire.ai/v6`。
- 已通过真实 `GET /v6/models` 响应核对后端目录，共纳入 53 个当前模型。
- 39 个聊天与推理模型可以在管理端选择；6 个图像、4 个视频、2 个 Embedding 和 2 个 Rerank 模型保留在后端能力目录，但不会误入聊天端点。
- 默认模型为 `glm-5.2`；真实连接测试通过，实测延迟 4435 ms。
- 模型配置必须经过“保存、测试、启用”三步；更换模型或凭证后自动停用并要求重新测试。
- API Key 使用企业 ID 作为附加认证数据执行 AES-GCM 加密；数据库、环境文件、浏览器存储、日志、回答证据和 Git 均不保存明文。
- Hermes 不挂载持久模型凭证；API 只为单次请求解密，并以带时间戳和请求 ID 的 HMAC 签名传递，重复请求会被拒绝。
- Anspire 不可用时明确失败，不回退到其他模型供应商，也不生成伪回答。

## 真实运行验收

### 数据与同步

- 四个数据域均为 `fresh`：商机 3000、交付 800、回款 12000、目标 600。
- 商机来源为 `simulated_feishu`；交付、回款和目标来源为 `simulated_generator`。
- 四域数据截止时间均为 `2026-07-25T18:00:00Z`，前端按上海时区显示为 7 月 26 日 02:00。
- 人工同步任务成功完成：读取 17051 条、写入 0 条、拒绝 0 条，证明无变化重跑保持幂等。
- 每日同步任务已启用，Cron 为 `0 2 * * *`，时区为 `Asia/Shanghai`。

### 经营问数

- 通过董事长账号发起真实经营问题，由 GLM-5.2 完成回答。
- 回答包含结构化结论、数据截止时间及商机、交付、回款、目标四类证据。
- 关键数字从受控工具读取，回答明确标注模拟数据，不把演示数据描述为客户真实数据。

### 当前会话文件

- 通过真实 DOCX 上传、异步解析、向量/关键词混合检索、GLM-5.2 回答和文件证据下钻完成闭环。
- 测试文件解析为 2 个分块，回答正确命中两项验证数字并引用文件名。
- 测试结束后已删除文件、解析结果、加密对象和测试会话，未留下验收样本。

### 身份与运行环境

- 本机只保留 `chairman@demo.local` 董事长账号和 `admin@demo.local` 企业管理员账号；临时 QA 用户、会话、文件和密钥占位均已清理。
- 管理端仅企业管理员可访问；董事长账号不能读取或修改模型配置。
- 浏览器验收确认登录、经营工作台、数据新鲜度、事业部范围和模拟数据标识均正常。
- Nginx 仅监听 `127.0.0.1:8080`，未向局域网或公网开放。

## 最小权限数据库角色

六个常驻服务使用独立数据库账号：

- API：应用数据读写，审计事件不可修改。
- MCP Hub：仅查询授权经营维表和事实表，不能读取用户或写入事实。
- Ingestion Worker：写入同步批次、事实、快照、状态和检查点。
- Scheduler：只管理计划、运行记录和任务队列，不读取经营事实。
- Assistant Worker：处理会话、消息、路由、证据和回答任务。
- File Worker：处理文件、解析结果、分块和文件任务。

原通用运行角色只供受控的一次性 Seed、Bootstrap 和源库管理命令使用。正向与反向权限测试均已通过；当前隔离为表级最小权限，任务队列内的 `job_type` 行级边界仍由 Worker 白名单控制。

## 自动化检查

- API：Ruff 通过；真实 PostgreSQL 测试 70 项全部通过。
- Worker：Ruff 通过；真实 PostgreSQL 测试 19 项全部通过。
- Hermes Runtime：Ruff 通过；6 项测试全部通过。
- 前端：TypeScript 检查通过；9 项生产契约测试通过；生产构建通过。
- 基础设施：Compose、安全边界、镜像固定、密钥职责、发布 bundle 和迁移 head 检查通过。
- 容器 smoke：网关、API live/ready、Alembic head、加密存储、MCP、Hermes 0.19.0、只读模型缓存和本机监听均通过。
- `git diff --check`：通过。

## 备份与恢复点

最终验收备份：

```text
backups/local-demo/20260727T174426Z-phase2-final
```

备份包含加密数据库、加密文件与签名清单；备份时应用受控静默，完成后已恢复运行。

## 尚需在具体客户部署时配置

- 客户准备符合标准 DDL 的脱敏 PostgreSQL，并提供只读账号和 TLS 连接信息。
- 如需现场展示真实飞书同步，配置客户或演示租户的飞书应用、只读运行凭证及目标多维表格；本次验收没有声称已连接外部真实飞书租户。
- 每日/每周简报自动生成与飞书消息推送属于第三阶段。
- 离线镜像、安装包、升级与回滚材料属于第五阶段。

## 可复验命令

```bash
./scripts/start.sh local-demo
./scripts/status.sh local-demo
./scripts/smoke-test.sh local-demo
./scripts/sync-now.sh local-demo demo-enterprise
```
